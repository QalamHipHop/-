{
  "name": "rial-platform",
  "private": true,
  "version": "0.1.0",
  "description": "RIAL — production-grade token launch platform",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "frontend",
    "backend",
    "payment-service",
    "notification-service",
    "analytics"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
